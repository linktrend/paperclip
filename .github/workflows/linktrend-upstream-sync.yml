name: LiNKtrend Sunday Upstream Sync

on:
  schedule:
    - cron: "0 18 * * 0"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upstream-sync:
    uses: linktrend/LiNKtrend-System/.github/workflows/reusable-fork-upstream-sync.yml@main
    with:
      upstream_url: https://github.com/paperclipai/paperclip.git
      upstream_branch: master
    secrets: inherit
